const body=document.body,navPanel=document.querySelector('.nav-panel'),menu=document.querySelector('.menu-toggle');
menu.addEventListener('click',()=>{const open=navPanel.classList.toggle('is-open');menu.setAttribute('aria-expanded',open)});
document.querySelectorAll('.nav-panel a').forEach(a=>a.addEventListener('click',()=>{navPanel.classList.remove('is-open');menu.setAttribute('aria-expanded','false')}));
document.querySelector('.nav-dropdown button').addEventListener('click',e=>{const parent=e.currentTarget.parentElement;const open=parent.classList.toggle('is-open');e.currentTarget.setAttribute('aria-expanded',open)});
function openModal(id){const modal=document.getElementById(id);const iframe=modal.querySelector('iframe[data-youtube-src]');if(iframe&&!iframe.src)iframe.src=iframe.dataset.youtubeSrc;modal.classList.add('is-open');modal.setAttribute('aria-hidden','false');body.classList.add('modal-open')}
function closeModal(modal){modal.classList.remove('is-open');modal.setAttribute('aria-hidden','true');const iframe=modal.querySelector('iframe[data-youtube-src]');if(iframe)iframe.src='';body.classList.remove('modal-open')}
document.querySelectorAll('[data-video-open]').forEach(b=>b.addEventListener('click',()=>openModal('video-modal')));
document.querySelectorAll('[data-plan-open]').forEach(b=>b.addEventListener('click',()=>{const img=document.querySelector(`[data-plan="${b.dataset.planOpen}"] img`);const modal=document.getElementById('plan-modal');modal.querySelector('img').src=img.src;modal.querySelector('img').alt=img.alt;openModal('plan-modal')}));
document.querySelectorAll('.gallery-item').forEach(item=>item.addEventListener('click',()=>{const modal=document.getElementById('gallery-modal');modal.querySelector('img').src=item.dataset.image;modal.querySelector('img').alt=item.querySelector('img').alt;modal.querySelector('figcaption').textContent=item.dataset.caption;openModal('gallery-modal')}));
document.querySelectorAll('[data-modal-close]').forEach(b=>b.addEventListener('click',()=>closeModal(b.closest('.modal'))));
document.querySelectorAll('.modal').forEach(m=>m.addEventListener('click',e=>{if(e.target===m)closeModal(m)}));
document.addEventListener('keydown',e=>{if(e.key==='Escape')document.querySelectorAll('.modal.is-open').forEach(closeModal)});
document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(b=>b.classList.remove('is-active'));button.classList.add('is-active');const filter=button.dataset.filter;document.querySelectorAll('.gallery-item').forEach(item=>item.classList.toggle('is-hidden',filter!=='all'&&!item.dataset.gallery.includes(filter)))}));
document.getElementById('inquiry-form').addEventListener('submit',e=>{e.preventDefault();e.currentTarget.querySelector('.form-status').textContent='Demo inquiry received. A live form destination will be added after client confirmation.';e.currentTarget.reset()});
document.querySelector('[data-pricelist]').addEventListener('click',()=>document.querySelector('#contact').scrollIntoView({behavior:'smooth'}));
document.querySelector('[data-map-placeholder]').addEventListener('click',e=>{e.preventDefault();alert('Google Maps link will be added once confirmed by the client.')});
