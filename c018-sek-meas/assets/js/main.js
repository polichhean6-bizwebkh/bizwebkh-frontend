const $=(s,c=document)=>c.querySelector(s), $$=(s,c=document)=>[...c.querySelectorAll(s)];
if(!document.querySelector('link[href="assets/css/refined.css"]'))document.head.insertAdjacentHTML('beforeend','<link rel="stylesheet" href="assets/css/refined.css">');if(!document.querySelector('link[href="assets/css/admin-mobile.css"]'))document.head.insertAdjacentHTML('beforeend','<link rel="stylesheet" href="assets/css/admin-mobile.css">');
function esc(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function basketCount(){return getBasket().reduce((sum,item)=>sum+item.qty,0)}
function basketQty(id){return getBasket().find(item=>item.id===id)?.qty||0}
function header(){return `<div class="utility">${SEK_CONFIG.phone}<span>Mon–Sat, 8:00 AM–5:00 PM</span><span>Product Inquiry & Wholesale Support</span></div><header><a class="brand" href="index.html"><b>SEK</b><span>MEAS<small>GRAINS & EDIBLE OILS</small></span></a><button class="nav-toggle" aria-label="Open navigation" aria-expanded="false">☰</button><nav><a href="index.html">Home</a><a href="products.html">Products</a><a href="about.html">About Us</a><a href="contact.html">Contact Us</a></nav></header>`}
function footer(){return `<footer><div><a class="brand" href="index.html"><b>SEK</b><span>MEAS<small>GRAINS & EDIBLE OILS</small></span></a><p>Supplying grains, cooking oils and packaged food products for Cambodian businesses.</p></div><div><h4>Visit us</h4><p>#07, Street 598, Tuol Sangke I<br>Russey Keo, Phnom Penh, Cambodia</p></div><div><h4>Contact</h4><p>${SEK_CONFIG.phone}<br>${SEK_CONFIG.email}</p><a class="text-link" href="${SEK_CONFIG.telegramUrl}" target="_blank">Telegram inquiry</a></div></footer><div class="copyright">© 2026 Sek Meas Grains & Edible Oils Co., Ltd.</div>`}
function mountShell(){const h=$('#site-header'),f=$('#site-footer');if(h)h.innerHTML=header();if(f)f.innerHTML=footer();document.body.insertAdjacentHTML('beforeend','<a class="floating-inquiry" href="inquiry.html" aria-label="Open inquiry list"><span class="floating-icon">▣</span><span class="floating-label">Inquiry List</span><b class="basket-count">0</b></a><div class="toast" role="status" aria-live="polite"></div>');const n=$('nav');$('.nav-toggle')?.addEventListener('click',e=>{n.classList.toggle('open');e.currentTarget.setAttribute('aria-expanded',n.classList.contains('open'))});window.addEventListener('basketchange',refreshInquiryUI);refreshInquiryUI()}
function setBasketQty(id,qty){let b=getBasket(),item=b.find(x=>x.id===id);qty=Math.max(0,Number(qty)||0);if(!qty){setBasket(b.filter(x=>x.id!==id));return}if(item)item.qty=qty;else b.push({id,qty});setBasket(b)}
function addToBasket(id,qty=1){setBasketQty(id,basketQty(id)+qty);return true}
function removeFromBasket(id){setBasket(getBasket().filter(x=>x.id!==id))}
function isSelected(id){return getBasket().some(x=>x.id===id)}
function refreshInquiryUI(){let n=basketCount();$$('.basket-count,.banner-count').forEach(x=>x.textContent=n);const f=$('.floating-inquiry');if(f)f.classList.toggle('is-visible',n>0);$$('.qty-stepper').forEach(stepper=>{let qty=basketQty(stepper.dataset.id),card=stepper.closest('.product-card');stepper.querySelector('[data-qty]').textContent=qty;let status=card?.querySelector('[data-selected]');if(status)status.textContent=`In inquiry: ${qty}`;card?.classList.toggle('is-selected',qty>0);let note=card?.querySelector('.added-label');if(note)note.hidden=!qty})}
function showToast(msg){const t=$('.toast');if(!t)return;t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2600)}
function productCard(p){let qty=basketQty(p.id);return `<article class="product-card ${qty?'is-selected':''}"><a href="product-detail.html?id=${encodeURIComponent(p.id)}"><img src="${esc(p.image)}" alt="${esc(p.name)}"></a><div class="product-copy"><span class="eyebrow">${esc(p.category)}</span><h3>${esc(p.name)}</h3><p>SKU ${esc(p.sku)} · ${esc(p.size)}</p><small class="added-label" ${qty?'':'hidden'}>Added to inquiry</small><div class="card-actions"><div class="qty-stepper" data-id="${esc(p.id)}" aria-label="Quantity for ${esc(p.name)}"><button type="button" data-qty-action="down" aria-label="Decrease quantity">−</button><output data-qty>${qty}</output><button type="button" data-qty-action="up" aria-label="Increase quantity">+</button></div><a class="btn btn-ghost btn-small" href="product-detail.html?id=${encodeURIComponent(p.id)}">Details</a></div><small class="selection-status" data-selected>In inquiry: ${qty}</small></div></article>`}
document.addEventListener('click',e=>{const b=e.target.closest('[data-qty-action]');if(!b)return;let stepper=b.closest('.qty-stepper'),id=stepper.dataset.id,qty=basketQty(id)+(b.dataset.qtyAction==='up'?1:-1);setBasketQty(id,qty);showToast(qty>0?'Inquiry quantity updated':'Product removed from your inquiry list');refreshInquiryUI()});document.addEventListener('DOMContentLoaded',()=>{if(!$('.admin'))mountShell();if($('.admin')){document.body.insertAdjacentHTML('afterbegin','<button class="admin-menu" aria-label="Open admin navigation" aria-expanded="false">☰ Menu</button>');$('.admin-menu').onclick=e=>{const side=$('.admin-side');side?.classList.toggle('open');e.currentTarget.setAttribute('aria-expanded',side?.classList.contains('open'))}}});
