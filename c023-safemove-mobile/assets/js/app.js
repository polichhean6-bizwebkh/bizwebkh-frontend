const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];function toast(msg){let t=$('.toast');if(!t){t=document.createElement('div');t.className='toast';document.body.append(t)}t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1800)}function valid(form){let ok=true;form.querySelectorAll('[required]').forEach(i=>{let f=i.closest('.field');if(!i.value.trim()||(i.type==='checkbox'&&!i.checked)){f?.classList.add('invalid');ok=false}else f?.classList.remove('invalid')});return ok}document.addEventListener('DOMContentLoaded',()=>{$$('[data-password]').forEach(b=>b.onclick=()=>{let i=$(b.dataset.password);i.type=i.type==='password'?'text':'password';b.textContent=i.type==='password'?'Show':'Hide'});$$('form[data-go]').forEach(f=>f.onsubmit=e=>{e.preventDefault();if(valid(f)){sessionStorage.setItem('smUser',f.querySelector('[name=name]')?.value||'Sokha Chea');location.href=f.dataset.go}});$$('[data-select]').forEach(e=>e.onclick=()=>{let group=e.dataset.select;$$(`[data-select="${group}"]`).forEach(x=>x.classList.remove('selected'));e.classList.add('selected');if(group==='delivery'&&$('#estimatePrice'))$('#estimatePrice').textContent=e.dataset.price||'$4.70'});$$('[data-tab]').forEach(e=>e.onclick=()=>{$$('[data-tab]').forEach(x=>x.classList.remove('active'));e.classList.add('active');toast(e.textContent+' orders shown')});$$('[data-promo]').forEach(e=>e.onclick=()=>{sessionStorage.setItem('promo',e.dataset.promo);toast('Promo '+e.dataset.promo+' selected')});$('#applyPromo')?.addEventListener('click',()=>{let c=$('#promoCode').value.trim().toUpperCase();if(c){sessionStorage.setItem('promo',c);toast('Promo code applied')}});$('#advance')?.addEventListener('click',()=>{let steps=$$('.step');let current=steps.findIndex(x=>x.classList.contains('current'));if(current<steps.length-1){steps[current].classList.remove('current');steps[current].classList.add('done');steps[current+1].classList.add('current');$('#trackStatus').textContent=steps[current+1].querySelector('b').textContent;toast('Delivery status updated')}});$('#logout')?.addEventListener('click',()=>{sessionStorage.clear();location.href='login.html'})});
